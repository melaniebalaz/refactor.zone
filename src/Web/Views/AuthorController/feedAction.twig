{% spaceless %}
	{% import "meta.twig" as meta %}
	{{ meta.rss(
		request.uri,
		author.name,
		'https://refactor.zone/author/' ~ author.slug,
		author.htmlBody|striptags,
		articles
	) }}
{% endspaceless %}